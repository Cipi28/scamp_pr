<!DOCTYPE html>
<html>
<head>
    <style>
        table, th, td {
            border:1px solid black;
        }
    </style>
</head>
<body>
<H1><center>Stations Manager</center></H1><br>
<a href="/{{ app.user ? 'logout' : 'login' }}" @click="isOpen = false">
{{ app.user ? 'Logout' : 'Login' }}
</a>
<br><br>
<table>
    <tr>
        <td>Station Name</td>
        <td>Location</td>
        <td>Details of the Station</td>
        <td>Delete Station</td>
    </tr>
    {% for station in stations %}
        <tr>
            <td>{{ station.getName() }}</td>
            <td>{{ station.getLocation() }}</td>
            <td><a href="{{ path('station_view', {'id':station.getId()} )}}"><center>Details</center></a></td>
            <td><a href="{{ path('delete_view', {'id':station.getId()} )}}"><center>Delete</center></a></td>
        </tr>
    {% endfor %}
</table>
<br>
<a href="/new-station">Add Station</a>
</body>
</html>